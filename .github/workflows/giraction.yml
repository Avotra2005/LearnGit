name: mine work

on:
  push: main

jogs:
  Learnt:
    name: Lint Code Base
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - uses: github/super-linter@v3
        env:
          GUTHUB_TOKEN: ${{secrets.GUTHUB_TOKEN}}
